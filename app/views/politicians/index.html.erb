
<div class="d-flex justify-content-around flex-wrap main-container">
  <% @politicians.each do |politician| %>
    <%= link_to politician_path(politician), class:"politician_card" do %>
      <div class="d-flex flex-column justify-content-center align-items-center autour ">
        <div class="politician-name">
          <h2><%= politician.name %> </h2>
        </div>
        <div class="card align-self" style="background-image: url( <%= cl_image_path politician.photo.key, height: 300, crop: :fill %>)">
        </div>
        <div class="popularity">
          <% politician.moyenne.times do %>
            <span class="fa fa-star filled" ></span>
          <% end %>
          <% (5 -politician.moyenne).times do %>
            <span class="fa fa-star" ></span>
          <% end %>
        </div>
      </div>
    <% end %>

  <% end %>
</div>

<%#     <li><%= link_to politician.bio, politician_path(politician) %>
 <%# <li><%= link_to politician.img_url, politician_path(politician) %>

<%# Line 6 cl_image_path politician.photo.key was added by Abdelhak as part of cloudinary setup %>
